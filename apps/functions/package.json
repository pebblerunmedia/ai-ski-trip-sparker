{
  "name": "@ski-sparker/functions",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "genkit:dev": "genkit start -- tsx --watch src/index.ts",
    "genkit:deploy": "firebase deploy --only functions",
    "deploy": "firebase deploy --only functions",
    "setup": "npm install -g firebase-tools && firebase login",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@genkit-ai/ai": "^0.5.17",
    "@genkit-ai/core": "^0.5.0",
    "@genkit-ai/firebase": "^0.5.0",
    "@genkit-ai/flow": "^0.5.17",
    "@genkit-ai/googleai": "^0.5.0",
    "@genkit-ai/vertexai": "^0.5.0",
    "firebase-admin": "^12.1.0",
    "firebase-functions": "^5.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": "20"
  }
}
